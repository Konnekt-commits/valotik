import React from 'react';
import ReactDOM from 'react-dom/client';
import RHInsertionApp from './rh-insertion-app';
import './index.css';

ReactDOM.createRoot(document.getElementById('rh-insertion-root')!).render(
  <React.StrictMode>
    <RHInsertionApp />
  </React.StrictMode>,
);
