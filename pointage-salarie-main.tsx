import React from 'react';
import ReactDOM from 'react-dom/client';
import PointageSalarieApp from './pointage-salarie-app';
import './index.css';

ReactDOM.createRoot(document.getElementById('pointage-salarie-root')!).render(
  <React.StrictMode>
    <PointageSalarieApp />
  </React.StrictMode>,
);
